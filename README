canopyEMBEDDED - open source IoT framework - embedded code
------------------------------------------------------------------
TO CLONE: git clone https://github.com/gregulator/canopy-embedded.git

PREQUISITES

    LIBRED
        git clone https://github.com/gregulator/silver-c.git

    LIBWEBSOCKETS
        sudo apt-get install cmake
        sudo apt-get install zlib1g-dev
        sudo apt-get install libssl-dev
        sudo apt-get install rpm
        sudo apt-get install g++
        git clone git://git.libwebsockets.org/libwebsockets
        mkdir build
        cd build
        cmake ..
        make
